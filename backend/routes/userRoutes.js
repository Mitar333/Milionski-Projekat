// const userControler= require('../controllers/userController');
// const express = require('express');
// const router = express.Router();

// // Route to get all users
// router.get('/', userControler.getAllUsers);
// // Route to get a user by ID
// router.get('/:id', userControler.getUserById);
// // Route to create a new user
// router.post('/', userControler.postUser);
// // Route to update a user's name
// router.put('/:id/name', userControler.updateUserName);
// // Route to update a user's email
// router.put('/:id/email', userControler.updateUserEmail);
// // Route to delete a user
// router.delete('/:id', userControler.deleteUser);
// // Export the router
// module.exports = router;
